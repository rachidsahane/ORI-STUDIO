# DEAD: the gate command is not in the file at all. The first thing the check
# must be able to say, and the only one of these the old whole-file grep also
# caught.
name: sample-dead-command-absent
on:
  pull_request:
jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - name: something else
        run: echo not the gate
  ci:
    needs: [fmt]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: aggregate
        run: echo aggregate
